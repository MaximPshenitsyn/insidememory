services:
  nginx:
    image: nginx:1.27-alpine
    container_name: insidememory-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./landings:/usr/share/nginx/html/landings:ro
    restart: unless-stopped
